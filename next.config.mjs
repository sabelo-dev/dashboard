/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['abzu-product-holder.s3.amazonaws.com','abzu-product-holder.s3.us-east-1.amazonaws.com'],
    },
  };
  
  export default nextConfig;